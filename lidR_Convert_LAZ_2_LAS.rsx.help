{"ALG_CREATOR": "Grzegorz Sapijaszko, grzegorz(at)sapijaszko.net",
 "ALG_DESC": "The script uses {lidR} package to convert bunch of *.LAZ files to uncompressed *.LAS, with optional index files. You can optionally add any filter string, '-keep_class 2 9' is default one",
  "Input": "An input directory with *.laz files", 
  "Output": "An output directory, where .las files are written",
  "Filter": "Optional - filter for points",
  "Create Index": "logical TRUE (default), if the index files has to be created during conversion. Index files speeds up all operations on *.LAS",
  "ALG_HELP_CREATOR": "Grzegorz Sapijaszko, grzegorz(at)sapijaszko.net"
  }
